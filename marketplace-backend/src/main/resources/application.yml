spring:
  application:
    name: marketplace-backend
  
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://yogeshjat8965_db_user:rSqxEps5KTz7o2Qp@vendorhub.whs5yyc.mongodb.net/marketplace?retryWrites=true&w=majority}
      database: marketplace
  
  servlet:
    multipart:
      # Sized for the largest inbox attachment (100MB video) plus headroom.
      # NOTE: this was previously 10MB, which silently rejected the existing
      # catalogue video (50MB) and PDF (20MB) uploads before they ever reached
      # application code — that pre-existing bug is fixed by this change too.
      max-file-size: 110MB
      max-request-size: 110MB

server:
  port: ${PORT:8080}

jwt:
  secret: ${JWT_SECRET:ThisIsAVeryLongSecretKeyForJWTTokenGenerationThatIsAtLeast256BitsLong12345678}
  expiration: 3600000  # 1 hour in milliseconds

cors:
  allowed-origins: ${ALLOWED_ORIGINS:http://localhost:3000,http://localhost:8085}

logging:
  level:
    com.marketplace: DEBUG
    org.springframework.security: DEBUG

cloudinary:
  cloud-name: dtzjzuoud
  api-key: 272113567425682
  api-secret: lon_Vq1ouhoZ0KnIZ1Ib6spT4Ik
